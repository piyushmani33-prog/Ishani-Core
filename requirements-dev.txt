# Ishani-Core / TechBuzz AI — Developer Dependencies
# Install alongside requirements.txt for local development and testing:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These packages are NOT needed in production.

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # async HTTP client used by TestClient

# Code quality
ruff>=0.4.0             # fast linter (replaces flake8/pyflakes/pycodestyle)

# Type checking (optional but recommended)
mypy>=1.9.0

# Local dev conveniences
python-dotenv>=1.0.0    # load .env in test helpers
